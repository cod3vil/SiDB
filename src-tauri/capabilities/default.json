{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DBLite 最小权限集：仅开放用到的 API（TDD §12 Tauri allowlist 最小化）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-confirm",
    "dialog:allow-message"
  ]
}
